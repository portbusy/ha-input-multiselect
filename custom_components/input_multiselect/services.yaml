set_options:
  name: Set options
  description: Completely overwrites the currently selected options. (Leave empty to clear all)
  target:
    entity:
      integration: input_multiselect
  fields:
    options:
      name: Options
      description: List of options to set (e.g., ['Kitchen', 'Living Room'] or 'Kitchen, Living Room').
      required: false
      selector:
        text:
          multiple: true

add_options:
  name: Add options
  description: Adds new options to the currently selected ones.
  target:
    entity:
      integration: input_multiselect
  fields:
    options:
      name: Options
      description: List of options to add.
      required: true
      selector:
        text:
          multiple: true

remove_options:
  name: Remove options
  description: Removes specific options from the currently selected ones.
  target:
    entity:
      integration: input_multiselect
  fields:
    options:
      name: Options
      description: List of options to remove.
      required: true
      selector:
        text:
          multiple: true